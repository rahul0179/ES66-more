const n = [2, 4, 6, 8];

const m = number => number * 2;


/* for (const i of n) {
    let result = m(i)
    n2.push(result);
} */

//const output = n.map(m)
const squre = n.map(x => x * x);
console.log(squre)
//console.log(output)